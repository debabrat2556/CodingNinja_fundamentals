First in your local machine 
create a folder inside the repository
create a file to be placed inside the created folder
git pull origin main
git add
git commit 
git push


****The below section returns the length of an integer value
int length = String.valueOf(binaryNo).length(); //returns lenght of binaryNo
System.out.println("length is "+length);